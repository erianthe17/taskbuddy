fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
# Pinned exactly: joblib artifacts are only guaranteed to load under the same
# scikit-learn version they were trained with (see model/*.meta.json).
scikit-learn==1.8.0
pandas>=2.2
joblib>=1.4
