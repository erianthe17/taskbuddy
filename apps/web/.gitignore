# Internal-only planning/spec notes (not part of the shipped app or public docs)
docs/

# Local dev journal — running log of work, not a shipped doc
.notes/

# Screenshots generated by running scripts/*.py test scripts locally
scripts/*.png
